version: "3.9"

networks:
  my-network:
    driver: bridge

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:80"
    volumes:
      - ./HealthConnect/output:/app/output
    networks:
      - my-network
  # front-end:
  #   build: ./front-end
  #   container_name: front_end_c
  #   ports:
  #     - "3000:3000"
  #   stdin_open: true
  #   tty: true
  #   volumes:
  #     - ./front-end:/usr/src/app/front_end
  #     - /usr/src/app/front-end/node_modules